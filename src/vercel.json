{
  "version": 2,
  "name": "blogs & posts",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ],
  "scripts": {
    "build": "[my-framework] build --output public"
  }
}